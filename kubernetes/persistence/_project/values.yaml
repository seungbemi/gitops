apps:
  - name: rook-ceph
    namespace: persistence
    path: kubernetes/persistence/rook-ceph
    sync: true

  - name: redis
    namespace: persistence
    path: kubernetes/persistence/redis
    sync: true

  - name: postgresql
    namespace: persistence
    path: kubernetes/persistence/postgresql
    sync: true

  - name: persistence-secrets
    namespace: persistence
    repoURL: git@github.com:seungbemi/gitops-secrets.git
    path: kubernetes/persistence/secrets
    sync: true
